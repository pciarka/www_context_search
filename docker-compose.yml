version: '3.1'
services:
  web:
    build: .
    ports:
      - "8501:8501"
    secrets:
      - OPENAI_API_KEY
      - QDRANT_URL
      - QDRANT_API_KEY

secrets:
  OPENAI_API_KEY:
    external: true
  QDRANT_URL:
    external: true
  QDRANT_API_KEY:
    external: true
